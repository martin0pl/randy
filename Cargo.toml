[package]
name = "randy"
version = "1.1.0"
authors = ["martin0pl"]
edition = "2024"

[profile.release]
strip = true
lto = true
codegen-units = 1

[dependencies]
clap = { version = "4.0", features = ["derive"] }
rand = "0.8"
